# Rotate before deploying.
PAYLOAD_SECRET=dev-secret-change-me-in-production

# Local Postgres via unix socket (peer auth as current OS user).
# For TCP instead: postgresql://USER:PASS@localhost:5432/unfiltered_dev
DATABASE_URI=postgresql:///unfiltered_dev?host=/var/run/postgresql
