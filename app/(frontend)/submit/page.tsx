import { SubmitScene } from "@/components/cinematic/pages/SubmitScene";

export const metadata = {
  title: "File your truth",
  description: "The whisper becomes a shout. A form, a story, a record.",
};

export default function SubmitPage() {
  return <SubmitScene />;
}
