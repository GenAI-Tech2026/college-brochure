import { VerifiedScene } from "@/components/cinematic/pages/VerifiedScene";

export const metadata = {
  title: "Verified. Never tracked.",
  description: "The forensic process by which a student review becomes verified truth.",
};

export default function VerifiedPage() {
  return <VerifiedScene />;
}
